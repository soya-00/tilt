{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the webview is allowed to ask the shell for. Deliberately short: the UI talks to the Python core over HTTP, not through the shell, so the only things it needs are dismissing the quick-capture window and handing a link to the browser. The opener is scoped to http and https — a journal has no business opening a file:// URL, and the brief's links come from feeds rather than from anyone trustworthy.",
  "windows": ["main", "capture"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "global-shortcut:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "http://*" }, { "url": "https://*" }]
    }
  ]
}
