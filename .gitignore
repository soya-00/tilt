# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
core/build/
.mypy_cache/
htmlcov/
.coverage

# Node
node_modules/
dist/
dist-ssr/
*.local
.vite/

# Tauri
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/schemas/
# The packaged sidecar is a build output; the directory is tracked because the
# bundler will not build against a resource path that does not exist.
apps/desktop/src-tauri/binaries/tilt-core/*
!apps/desktop/src-tauri/binaries/tilt-core/README.md

# Tilt runtime data — never commit a journal
.tilt-data/
*.db
*.db-shm
*.db-wal

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
